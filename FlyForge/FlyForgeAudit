#!/usr/bin/env bash
# TackleBox: FlyForgeAudit v1.1.0
# Wrapper script for the FlyForgeAudit.py companion module.

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$SCRIPT_DIR/FlyForgeAudit.py" "$@"
