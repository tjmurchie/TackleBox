[pytest]
testpaths = tests
addopts = -v --tb=short -m "not slow"
markers =
    slow: exercises real external tools (blastn/primer3 exhaustive search) and can take several minutes; excluded by default, run explicitly with `pytest -m slow` or `pytest -o addopts=` to include everything
